[package]
name = "devicehub-core"
version = "0.1.0"
edition = "2024"
description = "Host-independent domain models and service contracts for DeviceHub"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
